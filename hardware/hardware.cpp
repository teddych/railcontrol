#include <cstring>		//memset

#include "hardware.h"

namespace hardware {

  // start the thing
  int hardware::start(struct params &params) {
    return 0;
  }

  // stop the thing
  int hardware::stop() {
    return 0;
  }

} // namespace
