In the folder hardware are all the classes used for command control
specific belongings. The classes are loaded dynamically if they are
used.

To create your own command control copy the virt.cpp and virt.h and
modify to your needs.
